package leetcode;

public class p124 {
//    private int road = Integer.MIN_VALUE;
//    public int maxPathSum(TreeNode root) {
//        int res = nodeMax(root);
//        return Math.max(road,res);
//    }
//    private int nodeMax(TreeNode node){
//        if(node == null) return 0;
//        int left = nodeMax(node.left);
//        int right = nodeMax(node.right);
//        int side = Math.max(left,right) + node.val;
//        int treeSide = left + right + node.val;
//        road = Math.max(road, side);
//        road = Math.max(road, treeSide);
//        road = Math.max(road, node.val);
//        return Math.max(side,node.val);
//    }
}
