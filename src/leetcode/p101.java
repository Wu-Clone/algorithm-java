package leetcode;

public class p101 {
//    public boolean isSymmetric(TreeNode root) {
//        return (root == null) || (root.left == null && root.right == null) || compare(root.left, root.right);
//    }
//
//    private boolean compare(TreeNode l, TreeNode r) {
//        if (l == null && r == null)
//            return true;
//        if (l != null && r != null)
//            return (l.val == r.val) && compare(l.left, r.right) && compare(l.right, r.left);
//        return false;
//    }
}
